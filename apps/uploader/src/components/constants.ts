// Base API URL - can be configured based on environment // remove import.meta.env.VITE_API_BASE_URL if will not use vite
export const API_BASE_URL =
	import.meta.env.VITE_API_BASE_URL || "https://tinyimage.rubenabix.com";
